services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: recon
      POSTGRES_USER: recon
      POSTGRES_PASSWORD: recon
    ports:
      # 호스트에 이미 5432를 쓰는 Postgres(예: 로컬 설치본)가 있어 5433으로 매핑한다.
      - "5433:5432"
